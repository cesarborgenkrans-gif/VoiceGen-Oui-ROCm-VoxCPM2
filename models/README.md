# Models

Place local model snapshots here. The default VoxCPM2 path is:

```text
models/VoxCPM2/
```

Model weights are intentionally ignored by git. You can also keep models anywhere else and set `VOXCPM_MODEL_PATH`.
