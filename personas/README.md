```text
     /\_/\
    ( ^.^ )  counting good reasons on tiny fingies
     > ^ <
```

# Personas

Custom Persona Lab profiles are saved here by default:

```text
personas/presets_custom.json
```

The real custom file is user-owned runtime data and is ignored by git. Use `presets_custom.example.json` as a starting shape for sharing or manual editing.
